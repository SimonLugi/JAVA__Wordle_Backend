spring.application.name=Wordle Backend
spring.mvc.pathmatch.matching-strategy=ANT_PATH_MATCHER

# Database Configuration
# Change to 'update' if you want automatic schema management Disable because Flyway will do it
spring.jpa.hibernate.ddl-auto=validate
spring.sql.init.mode=never
spring.jpa.properties.hibernate.boot.allow_jdbc_metadata_access=false
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
# Variable to your selected Database Example is designed for MySQL 9+
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
hibernate.dialect.storage_engine=innodb
spring.jpa.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# MySQL Connection URL
spring.datasource.url=jdbc:mysql://${SQL_HOST:localhost}:${SQL_PORT:3306}/${SQL_DB:WordleDB}?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
# spring.flyway.url=jdbc:mysql://${SQL_HOST:localhost}:${SQL_PORT:3306}/${SQL_DB:WordleDB}?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true

# Credentials from Environment
spring.datasource.username=${SQL_USER:mySqlUser}
spring.datasource.password=${SQL_PASSWORD:mySqlPassword}

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.user=root
# ${SQL_USER:root}
spring.flyway.password=Wordle12345
# ${SQL_PASSWORD:Wordle12345}
spring.flyway.locations=classpath:db/migration

spring.flyway.schemas=${SQL_DB:WordleDB}
spring.flyway.connect-retries-interval=120s


# Hide sensitive information in error reports
server.error.include-stacktrace=never

# Springdoc Configuration
springdoc.override-with-generic-response=false

# Disable default error handling
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.web.servlet.error.ErrorMvcAutoConfiguration
server.error.whitelabel.enabled=false
server.error.path=/hdsafdsf

# Test purpose (logging)
# logging.level.org.springframework.boot=DEBUG
# logging.level.org.springframework.security=DEBUG
# logging.level.net.ict_campus.Wordle=DEBUG
# logging.level.org.springframework.web=DEBUG
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.orm.jdbc.bind=TRACE
# logging.level.org.hibernate.type.descriptor.sql=trace